<%- model_class = Trip -%>
<div class="page-header">
  <h1>Cart Recommendations for Your Trip</h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:children) %>:</strong></dt>
  <dd><%= @trip.children %></dd>
  <dt><strong><%= model_class.human_attribute_name(:adults) %>:</strong></dt>
  <dd><%= @trip.adults %></dd>
  <dt><strong><%= model_class.human_attribute_name(:city_of_origin) %>:</strong></dt>
  <dd><%= @trip.city_of_origin %></dd>
  <dt><strong><%= model_class.human_attribute_name(:pace) %>:</strong></dt>
  <dd><%= @trip.pace %></dd>
  <dt><strong><%= model_class.human_attribute_name(:month_of_departure) %>:</strong></dt>
  <dd><%= @trip.month_of_departure %></dd>
</dl>

<div class="span12">
  <h4>Basics:</h4>
  <ul class="thumbnails">
    <% @basics.each_pair do |product, quantity| %>
      <%= render :partial => 'suggested_product', :locals => { :p => product, :q => quantity } %>
    <% end %>
  </ul>
</div>

<div class="span12">
  <h4>Foodstuffs:</h4>
  <ul class="thumbnails">
    <% @foods.each_pair do |product, quantity| %>
      <%= render :partial => 'suggested_product', :locals => { :p => product, :q => quantity } %>
    <% end %>
  </ul>
</div>

<!-- <div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              trips_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_trip_path(@trip), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              trip_path(@trip),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div> -->
