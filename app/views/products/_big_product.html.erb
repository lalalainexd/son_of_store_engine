<div class="product_pic">
  <%= image_tag product.image.url(:xx_large) %>
</div>

<%= button_to 'Add to Cart', line_items_path(:product_id => product) %>
<h2 class="big_product_title" ><%= product.name %></h2>
<%= product.description %>
<%= product.price %>
