<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "OregonSale" %></title>
  <%= csrf_meta_tags %>

  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
  <!--[if lt IE 9]>
    <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
  <![endif]-->

  <%= stylesheet_link_tag "application", :media => "all" %>
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js" ></script>
</head>

<body>

  <!-- Signup links -->
  <div class="container">
    <div class="main_nav">
      <% if current_user %>
        Logged in
          <% if current_user && (current_user.role?(:admin) || current_user.role?(:superuser))%>
            <%= link_to "(admin mode)", admin_path %>
          <% end %>
        as <%= current_user.full_name %>.
        <%= link_to "Log out", logout_path %>
      <% else %>
        <%= link_to "Sign up", signup_path %> or
        <%= link_to "log in", login_path %>.
      <% end %>
    </div>

    <!-- Brand and main nav -->
    <div class="navbar">
      <div class="navbar-inner">
        <%= link_to image_tag("logo.png"), root_url, :class => "brand" %>
        <div class="cart">
          Cart
          <ul>
          <% if session[:cart_id] %>
            <li><%=cart_cost(session[:cart_id])%><br /><%= link_to "View", cart_path(session[:cart_id]) %></li>
            <li><%= link_to "Checkout", orders_path, :method => :post, :class => 'btn btn-success' %></li>
            <li><%= link_to image_tag("cart.png"), cart_path(session[:cart_id]) %></li>
          <% else %>
            <li><%= image_tag("cart.png") %></li>
          <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <div class="breaker" class="container"></div>

  <div class="container">

    <!-- Flash -->
    <div class="row">
      <div class="span12">
        <%= bootstrap_flash %>
      </div>
    </div>

    <%= yield %>
  </div>

  <%= javascript_include_tag "application" %>
</body>
</html>
